// Bulk file namer
// TODO: process args first

use core::panic;
use std::fs;
use std::fs::read_dir;
use std::fs::File;
use std::{env, path};
use std::path::{Path, PathBuf};
use std::io;
use std::io::prelude::*;

fn main() {

}
